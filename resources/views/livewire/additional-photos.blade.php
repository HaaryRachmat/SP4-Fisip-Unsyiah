{{-- Care about people's approval and you will be their prisoner. --}}
<div>

    Post id : {{ galeri_id }}


    {{-- @if (count($additionalPhotos))
    @foreach ($additionalPhotos as $item)
    <img src="{{ url('storage/additional_photos/'. $item->filename) }}" width="200px" />
    @endforeach
    @else
    <div class="py-3 px-5 bg-yellow-100 rounded-md text-sm border border-gray-200">
        <h1 class="text-yellow-800 font-semibold text-center ">Tidak Ada Photo</h1>
    </div>
    @endif --}}
</div>